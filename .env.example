# Hytale Server Configuration
# Copy this file to .env and customize as needed

# ============================================
# AUTO-DOWNLOAD SETTINGS
# ============================================
# Automatically download server files on first start
# Set to "false" if you want to provide Server/ and Assets.zip manually
AUTO_DOWNLOAD=true

# ============================================
# JAVA SETTINGS
# ============================================
# JVM heap size and garbage collection options
# Adjust -Xms (initial) and -Xmx (maximum) based on available RAM
# Recommended: 4GB minimum for small servers, 8GB+ for larger ones
JAVA_OPTS=-Xms2G -Xmx4G -XX:+UseG1GC -XX:+ParallelRefProcEnabled -XX:MaxGCPauseMillis=200

# ============================================
# SERVER SETTINGS
# ============================================
# Hytale server command-line arguments
# Change port, view distance, enable backups, etc.
SERVER_OPTS=--assets Assets.zip --bind 0.0.0.0:5520

# Examples of additional options:
# SERVER_OPTS=--assets Assets.zip --bind 0.0.0.0:5520 --backup --backup-frequency 60
# SERVER_OPTS=--assets Assets.zip --bind 0.0.0.0:5520 --max-view-distance 8

# Server port (must match the port in SERVER_OPTS above)
SERVER_PORT=5520

# ============================================
# PERFORMANCE OPTIMIZATIONS
# ============================================
# Enable Ahead-Of-Time cache for faster startup (~30-50% improvement)
# Uncomment to enable:
# AOT_CACHE=-XX:AOTCache=Server/HytaleServer.aot

# ============================================
# DEVELOPMENT SETTINGS
# ============================================
# Disable Sentry crash reporting during plugin development
# Uncomment to disable:
# DISABLE_SENTRY=true

# ============================================
# RESOURCE LIMITS
# ============================================
# Docker resource limits (adjust based on your host)
CPU_LIMIT=4
MEMORY_LIMIT=6G
CPU_RESERVATION=2
MEMORY_RESERVATION=4G
